<%- contentFor('body') %>
  <div class="jumbotron text-center">
     <h1>O Time!</h1>
     <div class="row">
     	<% for (user of usuarios){%>
     	  <div class="col-sm-4">
     	  	<h2><%= user.name %>
     	  	<img class="img-responsive" style="height: 250px" src="<%= user.avatar %>">
     	  </div>

     	<% } %>
     </div>
  </div>